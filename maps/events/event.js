class Event {
    
    constructor(map) {
        this.dispose = false;
        this.executed = false;
        this.map = map;
    }
    
    update(deltatime) {
        
    }
    
    render(context) {
        
    }
}
